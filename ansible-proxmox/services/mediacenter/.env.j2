# User and Group IDs (find yours by running 'id' in your terminal)
PUID={{ PUID }}
PGID={{ PGID }}

# Timezone
TZ={{ TZ }}

# Base directory for container configuration data
CONFIG_DIR={{ CONFIG_DIR }}

# Base directory for media, downloads, and storage
DATA_DIR={{ DATA_DIR }}

# Plex claim token (obtain at https://plex.tv/claim)
PLEX_CLAIM={{ plex_claim }}